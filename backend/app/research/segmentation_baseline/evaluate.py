from __future__ import annotations


def evaluate_placeholder() -> dict:
    return {
        "task": "segmentation_baseline",
        "evaluated": False,
        "reason": "Research placeholder only.",
        "research_only": True,
    }
